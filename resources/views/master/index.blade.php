<x-app-layout><x-slot:header>{{ $title }}</x-slot:header><x-page-header eyebrow="Data master" :title="$title" description="Kelola kategori agar data operasional tetap konsisten." /><div class="grid gap-6 lg:grid-cols-3"><x-panel :title="$title" class="lg:col-span-2"><div class="space-y-3">@forelse($items as $item)<div class="rounded-lg border border-line p-4"><form class="grid gap-3 sm:grid-cols-4" method="POST" action="{{ route($route.'.update', $item) }}">@csrf @method('PUT')<input class="input !mt-0" aria-label="Nama kategori" name="name" value="{{ $item->name }}"><input class="input !mt-0 sm:col-span-2" aria-label="Deskripsi kategori" name="description" value="{{ $item->description }}"><button class="btn-secondary" type="submit">Simpan</button></form><div class="mt-3 flex items-center justify-between text-xs text-muted"><span>Dipakai {{ $item->tickets_count }} tiket</span><form id="delete-{{ $item->id }}" method="POST" action="{{ route($route.'.destroy', $item) }}">@csrf @method('DELETE')<button class="font-semibold text-red-700" type="submit">Hapus</button></form></div></div>@empty<x-empty-state title="Belum ada kategori" description="Tambahkan kategori pertama menggunakan formulir." icon="tag" />@endforelse</div></x-panel><x-panel title="Tambah kategori" class="self-start"><form class="space-y-4" method="POST" action="{{ route($route.'.store') }}">@csrf<div><label class="label" for="category_name">Nama</label><input id="category_name" class="input" name="name" required></div><div><label class="label" for="category_description">Deskripsi</label><textarea id="category_description" class="input" name="description" rows="4"></textarea></div><button class="btn-primary w-full" type="submit">Tambah kategori</button></form></x-panel></div></x-app-layout>
