<x-app-layout>
    <x-slot:header>Departemen</x-slot:header>
    <x-page-header eyebrow="Administrasi" title="Daftar departemen" description="Kelompokkan pengguna berdasarkan unit kerja perusahaan." />
    <div class="grid gap-6 lg:grid-cols-3"><div class="lg:col-span-2"><div class="table-wrap"><table class="table"><thead><tr><th>Nama</th><th>Deskripsi</th><th>Pengguna</th><th>Aksi</th></tr></thead><tbody>@forelse($departments as $department)<tr><td class="font-semibold text-ink">{{ $department->name }}</td><td class="!whitespace-normal text-muted">{{ $department->description }}</td><td>{{ $department->users_count }}</td><td><div class="flex gap-3"><a class="font-semibold text-accent" href="{{ route('departments.edit', $department) }}">Edit</a><form method="POST" action="{{ route('departments.destroy', $department) }}">@csrf @method('DELETE')<button class="font-semibold text-red-700" type="submit" onclick="return confirm('Hapus departemen?')">Hapus</button></form></div></td></tr>@empty<tr><td colspan="4" class="!p-0"><x-empty-state title="Belum ada departemen" description="Tambahkan departemen pertama menggunakan formulir." icon="building" /></td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $departments->links() }}</div></div>
        <x-panel title="Tambah departemen" description="Buat unit kerja baru." class="self-start"><form class="space-y-4" method="POST" action="{{ route('departments.store') }}">@csrf<div><label class="label" for="department_name">Nama</label><input id="department_name" class="input" name="name" required></div><div><label class="label" for="department_description">Deskripsi</label><textarea id="department_description" class="input" name="description" rows="4"></textarea></div><button class="btn-primary w-full" type="submit">Tambah departemen</button></form></x-panel>
    </div>
</x-app-layout>
