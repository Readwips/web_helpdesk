<x-app-layout>
    <x-slot:header>Penugasan Aset</x-slot:header>
    <div class="mx-auto max-w-2xl"><a class="mb-6 inline-flex items-center gap-2 text-sm font-semibold text-muted hover:text-accent" href="{{ route('assets.show', $asset) }}"><x-icon name="arrow" size="16" class="rotate-180" /> Detail aset</a><x-page-header eyebrow="Penugasan perangkat" :title="'Tugaskan '.$asset->asset_code" description="Catat pengguna dan jenis penggunaan aset ini." />
        <form class="panel" method="POST" action="{{ route('assets.assign.store', $asset) }}">@csrf<div class="panel-body space-y-5"><div><label class="label" for="user_id">Pengguna</label><select id="user_id" class="input" name="user_id" required><option value="">Pilih pengguna</option>@foreach($users as $user)<option value="{{ $user->id }}">{{ $user->name }} &mdash; {{ $user->email }}</option>@endforeach</select></div><div><label class="label" for="status">Jenis penggunaan</label><select id="status" class="input" name="status"><option value="digunakan">Digunakan tetap</option><option value="dipinjamkan">Dipinjamkan</option></select></div><div><label class="label" for="notes">Catatan</label><textarea id="notes" class="input" name="notes" rows="4" placeholder="Kebutuhan penggunaan atau informasi serah terima"></textarea></div></div><div class="border-t border-line bg-[#FAFAF8] px-5 py-4 sm:px-6"><button class="btn-primary w-full" type="submit">Konfirmasi penugasan</button></div></form>
    </div>
</x-app-layout>
